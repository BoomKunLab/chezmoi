[user]
    name  = BoomKunLab
    email = 80441830+BoomKunLab@users.noreply.github.com

[alias]
    vacuum = "!f () { git branch --merged|egrep -v '\\*|develop|main|master' | xargs git branch -d; git fetch --prune; };f"

[ghq]
    root = ~/.gitrepos
